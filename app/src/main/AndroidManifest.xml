<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coldbeer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.TelaMeusPedidos"></activity>
        <activity android:name=".activity.TelaListaProdutos" />
        <activity android:name=".activity.TelaItem" />
        <activity android:name=".activity.TelaFinalizarCompra" />
        <activity android:name=".activity.TelaCarrinho" />
        <activity android:name=".activity.TelaInicio" />
        <activity android:name=".activity.TelaPerfilUsuario" />
        <activity android:name=".activity.TelaCadastro" />
        <activity android:name=".activity.TelaLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>