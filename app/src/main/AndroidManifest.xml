<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coldbeer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TelaListaProdutos"></activity>
        <activity android:name=".TelaItem" />
        <activity android:name=".TelaFinalizarCompra" />
        <activity android:name=".TelaCarrinho" />
        <activity android:name=".TelaInicio" />
        <activity android:name=".PerfilUsuario" />
        <activity android:name=".TelaCadastro" />
        <activity android:name=".TelaLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>